for (let i = 0; i < 5; i++) {
    console.log(`Iteração: ${i}`);
    }
    
    let contador = 0;
    while (contador < 5) {
    console.log(`Iteração: ${contador}`);
    contador++;
    }
    
    let num = 5;
    do {
    console.log(`Número: ${num}`);
    num++;
    } while (num < 5);
    